# === Flask App Secrets ===
SECRET_KEY=my_app_is_gonna_rock

# === Email (Gmail SMTP) ===
EMAIL_USER=youremail@gmail.com
EMAIL_PASS=your_app_password

# === Database ===
# (Optional: if using remote DB instead of local sqlite)
DATABASE_URL=sqlite:///users.db
